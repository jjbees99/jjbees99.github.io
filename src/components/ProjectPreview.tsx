import { Link } from 'react-router-dom';import type { Project } from '../types/content';import { ProjectMedia } from './ProjectMedia';import { projectRoute } from '../utils/routes';
export function ProjectPreview({project,index}:{project:Project;index:number}){const media=<ProjectMedia project={project}/>;return <article className={`project-preview project-preview--${index%3}`}>{project.externalUrl?<a className="preview-image" href={project.externalUrl} target="_blank" rel="noreferrer" aria-label={`Visit the ${project.title} website`}>{media}</a>:<Link className="preview-image" to={projectRoute(project.slug)}>{media}</Link>}<div className="preview-copy"><span className="project-no">{String(index+1).padStart(2,'0')} / {project.year}</span><h2><Link to={projectRoute(project.slug)}>{project.title}</Link></h2><p>{project.summary}</p><div className="tagline"><span>{project.categories.join(' · ')}</span><span>{project.tags.join(' · ')}</span></div><Link className="text-link" to={projectRoute(project.slug)}>View project <span aria-hidden="true">↗</span></Link></div></article>}

import { Link } from 'react-router-dom';import type { VideoItem } from '../types/content';import { embedUrl } from '../utils/media';
export function VideoCard({item,index}:{item:VideoItem;index:number}){const remote=item.video.type!=='local';return <article className="video-card"><div className="video-frame">{remote?<iframe src={embedUrl(item.video.type as 'youtube'|'vimeo',item.video.src)} title={item.title} loading="lazy" allow="fullscreen; picture-in-picture"/>:<video controls preload="none" poster={item.poster}><source src={item.video.src} type="video/mp4"/><track kind="captions" label="English" src={`${import.meta.env.BASE_URL}media/videos/captions-placeholder.vtt`} srcLang="en" default/>Your browser does not support video.</video>}</div><div className="video-meta"><span>{String(index+1).padStart(2,'0')} — {item.year}</span><h2>{item.title}</h2><p>{item.caption}</p><Link to={`/projects/${item.relatedProjectSlug}`}>Related project ↗</Link>{item.duration&&<span>{item.duration}</span>}</div></article>}

import { useCallback,useRef } from 'react';import { Link } from 'react-router-dom';import { Logo } from './Logo';import { siteConfig } from '../content/siteConfig';import { useFocusTrap } from '../hooks/useFocusTrap';import { useScrollLock } from '../hooks/useScrollLock';
export function MenuOverlay({open,onClose}:{open:boolean;onClose:()=>void}){const ref=useRef<HTMLDivElement>(null);const close=useCallback(onClose,[onClose]);useFocusTrap(ref,open,close);useScrollLock(open);if(!open)return null;return <div ref={ref} className="menu-overlay" role="dialog" aria-modal="true" aria-label="Site menu"><div className="menu-top"><Logo inverse/><button className="button cream" onClick={close}>Close</button></div><nav className="menu-links" aria-label="Menu">{siteConfig.navigation.map((n,i)=><Link key={n.to} style={{'--i':i} as React.CSSProperties} to={n.to} onClick={close}>{n.label}</Link>)}</nav><div className="menu-bottom"><div><a href={siteConfig.githubUrl}>GitHub</a><a href={siteConfig.linkedinUrl}>LinkedIn</a><a href={siteConfig.cvUrl}>Download CV</a></div><span>© {siteConfig.name} {siteConfig.copyrightYear}</span></div></div>}

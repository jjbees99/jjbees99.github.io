import { useMemo,useState } from 'react';import { projects } from '../content/projects';import { ProjectPreview } from '../components/ProjectPreview';
const filters=['All','Digital Art','Engineering','Bioengineering','Robotics','Software','Research','Video'];
export function ProjectsPage(){const [active,setActive]=useState('All');const shown=useMemo(()=>active==='All'?projects:projects.filter(p=>p.categories.includes(active)),[active]);return <main id="main" className="projects-page"><header className="page-title"><p>Index / {projects.length} studies</p><h1>PROJECTS</h1><p>Experiments, instruments, analyses and prototypes—documented as working processes rather than polished myths.</p></header><div className="filters" role="group" aria-label="Filter projects">{filters.map(f=><button className={f===active?'active':''} aria-pressed={f===active} onClick={()=>setActive(f)} key={f}>{f}</button>)}</div><div className="project-list">{shown.map((p,i)=><ProjectPreview key={p.slug} project={p} index={i}/>)}</div></main>}

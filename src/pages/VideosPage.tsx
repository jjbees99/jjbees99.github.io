import { videos } from '../content/videos';import { VideoCard } from '../components/VideoCard';export function VideosPage(){return <main id="main" className="videos-page"><header className="page-title"><p>Moving image / field notes</p><h1>VIDEOS</h1><p>Tests, demonstrations and observations. Playback waits until you ask for it.</p></header>{videos.map((v,i)=><VideoCard key={v.id} item={v} index={i}/>)}</main>}
